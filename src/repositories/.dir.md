# Backend/main/src/repositories/

- DynamoDB アクセス層。テーブル名は `common/config.TABLE_NAME` を使用する
- Decimal → float 変換はこの層で行い、上位層に Decimal を露出させない
- テーブル設計は `Backend/main/docs/database_design.md` を参照
- アクセスパターンを変更・追加した場合は `Backend/main/docs/database_design.md` も更新すること
