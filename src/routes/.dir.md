# Backend/main/src/routes/

- HTTP リクエストの受付とレスポンス返却のみを担当する。ビジネスロジックは services に委譲する
- Powertools の `Router` を使用する
- 新規ルートファイル追加時は `app.py` に `include_router` で登録すること
- API 定義は `docs/openapi_main.yaml` に従う
- エンドポイントの追加・変更は親リポジトリの `docs/openapi_main.yaml` に影響するため、開発者に報告すること
