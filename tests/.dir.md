# Backend/main/tests/

- pytest + moto で AWS サービス（DynamoDB, Cognito）をモックしてテストする
- 外部 API（FRED, yfinance）もモックする。実際の外部通信は行わない
- テストは `tests/local/` に配置する。conftest.py に共通フィクスチャを定義する
- 影響範囲: `src/` の routes・services・repositories を変更した場合、対応するテストも更新すること
